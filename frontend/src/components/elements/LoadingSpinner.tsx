import '../../../public/styles/loading-spinner.css'


/**
 * Component for rendering loading spinner while
 * tasks are being fetched from database
 * @returns {React.JSX.Element} LoadingSpinner
 */
export default function LoadingSpinner() {
    return (
        <div className='loader'>
        </div>
    )
}
